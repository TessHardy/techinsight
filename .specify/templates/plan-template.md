# 技术方案：{feature_name}

> 基于 spec.md 的验收标准，描述 HOW

## 方案概述

{一句话总结技术方案}

## 技术选型

| 需求 | 方案 | 理由 | 替代方案 |
|------|------|------|----------|
| {需求1} | {选型} | {为什么选这个} | {没选的方案} |
| {需求2} | {选型} | {为什么选这个} | {没选的方案} |

## 实现步骤

### 步骤 1: {名称}

- 文件：`{文件路径}`
- 操作：{新增/修改} {什么}
- 关键代码：
  ```{lang}
  // 伪代码或关键片段
  ```

### 步骤 2: {名称}

- 文件：`{文件路径}`
- 操作：{新增/修改} {什么}
- 关键代码：
  ```{lang}
  // 伪代码或关键片段
  ```

### 步骤 3: {名称}

- 文件：`{文件路径}`
- 操作：{新增/修改} {什么}
- 关键代码：
  ```{lang}
  // 伪代码或关键片段
  ```

## 数据流

```
用户操作 → 事件处理 → 数据更新 → DOM/Canvas 渲染
   ↓            ↓           ↓            ↓
 {具体事件}  {函数名}   {变量/状态}   {UI 组件}
```

## 数据模型

{如有新增数据结构，在此定义}

```javascript
// 示例
var NEW_DATA = {
  key: {type: 'string', default: '', desc: '描述'},
  value: {type: 'number', default: 0, desc: '描述'}
};
```

存储位置：{localStorage key / 内存变量 / 无持久化}

## 状态管理

| 状态 | 类型 | 初始值 | 更新时机 | 持久化 |
|------|------|--------|----------|--------|
| {state1} | {string/number/boolean} | {值} | {事件} | {是/否} |

## 风险与缓解

| 风险 | 概率 | 影响 | 缓解策略 |
|------|------|------|----------|
| {风险描述} | 高/中/低 | 高/中/低 | {具体措施} |

## 性能影响

- 预估新增代码行数：{N} 行
- 预估渲染影响：{无 / <16ms / 需节流}
- 优化措施：{无 / requestAnimationFrame / 懒加载 / 虚拟列表}
- 性能预算校验：{当前 N KB + 新增 N KB = 总计 N KB，是否超标}

## Constitution 合规检查

- [ ] 架构约束：{合规 / 需豁免，原因}
- [ ] 编码规范：{合规 / 需豁免，原因}
- [ ] 依赖策略：{合规 / 需豁免，原因}
- [ ] 性能预算：{合规 / 需豁免，原因}
- [ ] 安全约束：{合规 / 需豁免，原因}
